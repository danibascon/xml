#!/usr/bin/python
# -+- coding: utf-8 .*.

import sys
from lxml import etree

arbol = etree.parse("/home/dani/Documentos/eventos.xml")
raiz = arbol.getroot()




contenido = raiz.findall("contenido")
for x in contenido:
	evento_nombre = x.findall("atributos idiomas/atributo nombre")
	for y in evento_nombre:
		print y





#provincias = arbol.findall('provincia')
#for provincia in provincias:
#	localidades = provincia.findall('localidades/localidad')
#	for localidad in localidades:
#		print localidad.text